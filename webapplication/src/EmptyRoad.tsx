const EmptyRoad = () => (
  <div 
    className="flex flex-col w-20 content-around items-center bg-gray-500" 
    // style={{ transform: `rotate(${rotation}deg)` }}
  >
  </div>
);

export default EmptyRoad;
